# Frozen synthetic brief

Synthetic teaching packet, not real-world claims: An internal engineering workshop needs exactly 6 slides including opening, to explain why an asynchronous work queue can reduce request latency yet increase completion latency or cause duplicate side effects. Audience: product engineers unfamiliar with distributed workers. No budget decision or investment recommendation is requested. Simplified system: client sends job to API; API saves durable job and responds 202 with job ID; broker delivers job to worker; worker calls email service then acknowledges broker. If worker crashes after email succeeds but before acknowledgment, broker may deliver again. At-least-once delivery means eventual redelivery after missing acknowledgment, not guaranteed exactly-once execution. Queue receives 100 jobs/s; each worker can complete 30 jobs/s in the idealised steady-state model. A measured 2-minute burst is 140 jobs/s with four workers; initial queue zero and capacity is constant for the exercise. API response P95 changes from 1.8s synchronous to 120ms asynchronous in one controlled load test; end-to-end completion P95 rises from 1.8s to 8s in that same test. These percentiles cannot be subtracted to infer a P95 queue wait. A durable job ID plus a unique idempotency key shared with the email provider can suppress repeat sends only if the provider enforces it over a sufficient retention window; a local 'sent' flag alone has a crash gap unless the side effect can join the same transaction. A poison job that fails deterministically should stop consuming unbounded retries, but retry counts/backoff/retention depend on service requirements not supplied. Explain the mechanism, use one worked backlog calculation, make recovery/correctness limits visible, and end with a concise diagnostic checklist for engineers. Keep teaching progression; do not force a recommendation-led executive summary, financial framing, or one chart form across pages.

Deliver the proposed story and structure only: communication objective, problem logic, complete dot-dash, storyboard/exhibit rationale, and a source/assumption ledger. Planning is auto-approved; do not render or export. Use only the supplied synthetic evidence. Do not browse.
